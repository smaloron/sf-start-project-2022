<h4>Liste des catégories</h4>
<ul class="list-group">
    {% for category in categoryList %}
        <li class="{{  html_classes('list-group-item',
            {'active': category == currentCategory})}}">
            <a href="{{ path('product_by_category', {'category': category}) }}"
               class="list-group-item-action">
                {{ category }}
            </a>
        </li>
        {#
        <li class="list-group-item {{ category == currentCategory ? 'active': '' }}">
            <a href="{{ path('product_by_category', {'category': category}) }}"
               class="list-group-item-action">
                {{ category }}
            </a>
        </li>
        #}
        {#
        {% if category == currentCategory %}
            <li class="list-group-item active">
                <a href="{{ path('product_by_category', {'category': category}) }}"
                class="list-group-item-action">
                    {{ category }}
                </a>
            </li>
        {% else %}
            <li class="list-group-item">
                <a href="{{ path('product_by_category', {'category': category}) }}"
                   class="list-group-item-action">
                    {{ category }}
                </a>
            </li>
        {% endif %}
        #}

    {% endfor %}
</ul>