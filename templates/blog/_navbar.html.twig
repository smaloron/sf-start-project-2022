<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('blog_home') }}">Mon blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('blog_list') }}">
                        tous les articles
                    </a>
                </li>

                {% if is_granted('ROLE_AUTHOR') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('blog_new_article') }}">
                            Nouvel article
                        </a>
                    </li>
                {% endif %}

                {% if app.user is not null %}
                    <li class="navbar-text">
                        Bonjour {{ app.user.fullName }}
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">
                            Déconnexion
                        </a>
                    </li>
                {% else  %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            Connexion
                        </a>
                    </li>
                {% endif %}

            </ul>
            <form class="d-flex" action="{{ path('blog_search') }}" method="get">
                <input class="form-control me-2" type="search" placeholder="votre recherche ici"
                       aria-label="Search" name="search"
                       value="{{ app.request.query.get('search') }}">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>